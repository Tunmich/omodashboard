1===	.venv\Scripts\Activate
====	deactivate


2===	python main.py


3===	python health_check.py --ping

4===	python utils/preflight_checker.py

5=== SHOWS MENU ====	python bootstrap.py

6==== TELEGRAM SESSION ID GEN ===python gen_session.py
Run it once:

You’ll enter the code Telegram sends, and it will generate a user_session.session file.

python utils/id_resolver.py
You can continue adding more without issue — the resolver will append them automatically on next scan.

7===	pip install --upgrade pip 
 	pip install -r requirements.txt
FOR GROUP CHAT ID RETRIEVER

Run:

bash
python gen_session.py
Enter:

The verification code from Telegram

Your Telegram password (if prompted)

=======python utils/chat_id_resolver.py

FOR SEARCHING 
grep -R "phantom_to_evm" . | grep -v __pycache__

